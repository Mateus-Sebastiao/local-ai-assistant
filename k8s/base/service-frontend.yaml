apiVersion: v1
kind: Service
metadata:
  name: service-frontend
  namespace: devops-assistant
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
