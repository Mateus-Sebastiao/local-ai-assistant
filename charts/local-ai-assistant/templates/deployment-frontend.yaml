apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "devops-assistant.fullname" . }}-deployment-frontend
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "devops-assistant.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  replicas: {{ .Values.frontend.replicaCount }}
  selector:
    matchLabels:
      {{- include "devops-assistant.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: frontend
  template:
    metadata:
      labels:
        {{- include "devops-assistant.labels" . | nindent 8 }}
        app.kubernetes.io/component: frontend
    spec:
      containers:
        - name: {{ .Chart.Name }}-frontend
          image: {{ .Values.frontend.image.repository }}:{{ .Values.frontend.image.tag }}
          ports:
            - containerPort: {{ .Values.frontend.service.targetPort }}
          envFrom:
            - configMapRef:
                name: {{ include "devops-assistant.fullname" . }}-config
